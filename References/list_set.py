# tuple 変更不可のシーケンス
# シーケンス　順番に処理するデータ構造、リスト、タプル、レンジ
# mutable変更可能←→immutable変更不可

# 要素の存在を確認する
a = []
print(x in a)
print(x not in a)

# マッチする要素の個数確認
q.count('a')

# 要素の削除
q.clear()

# 挿入
q.insert(index, x)

# 最初にマッチした値を削除する
q.remove('a')
